# GitHub relay MCP (server-side only — set in Vercel dashboard, never in Claude)
GITHUB_PAT=github_pat_...
GITHUB_REPO=Casper-Schepkens/personal-website

# Bearer token for Claude custom connector (Authorization: Bearer <secret>)
MCP_BRIDGE_SECRET=genereer-een-lang-geheim
