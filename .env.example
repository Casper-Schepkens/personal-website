# Claude MCP bridge (server-side only — Vercel env vars)
MCP_BRIDGE_SECRET=genereer-een-lang-geheim

# Cursor Automation webhook (from cursor.com/automations after saving)
CURSOR_AUTOMATION_WEBHOOK_URL=https://api2.cursor.sh/automations/webhook/...
CURSOR_AUTOMATION_AUTH_TOKEN=crsr_...
